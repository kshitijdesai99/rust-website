# This file shows what environment variables our app needs
# Copy this file to ".env" and fill in your actual values
# Environment variables are like settings you can change without modifying code

# Database Configuration
# This tells our app where to find the database
DATABASE_URL=sqlite:backend.db

# Server Configuration  
# These control where our web server runs
HOST=0.0.0.0    # Listen on all network interfaces (0.0.0.0) or just localhost (127.0.0.1)
PORT=3000       # What port to run on (3000 is common for development)

# Logging Configuration
# This controls how detailed our log messages are
# Options: error, warn, info, debug, trace (from least to most detailed)
RUST_LOG=debug